# ── RETIRED ──────────────────────────────────────────────────────────────────
# Render deployment has been replaced by deploy-gcp.yml (Cloud Run).
# This file is kept for historical reference only and will never trigger.
# ─────────────────────────────────────────────────────────────────────────────
name: Deploy to Render (retired)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is retired. Use deploy-gcp.yml instead."
        required: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration notice
        run: |
          echo "This workflow is retired."
          echo "Deployment now uses GCP Cloud Run via .github/workflows/deploy-gcp.yml"
          exit 1
