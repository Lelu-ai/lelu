# ── RETIRED ──────────────────────────────────────────────────────────────────
# Render deployment is retired. Production deploys use AWS ECS Fargate.
# See infrastructure/aws/ for the Terraform setup and deployment guide.
# This file is kept for historical reference only and will never trigger.
# ─────────────────────────────────────────────────────────────────────────────
name: Deploy to Render (retired)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is retired. See infrastructure/aws/ for the current deploy path."
        required: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Migration notice
        run: |
          echo "This workflow is retired."
          echo "Production deployment uses AWS ECS Fargate."
          echo "See infrastructure/aws/README.md for the current deploy path."
          exit 1
